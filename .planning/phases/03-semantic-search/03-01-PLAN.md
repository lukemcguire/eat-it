---
phase: 03-semantic-search
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_embedding_service.py
  - src/eat_it/services/embedding.py
  - alembic/versions/xxx_add_recipe_embeddings.py
autonomous: true
requirements:
  - SEARCH-01

must_haves:
  truths:
    - Recipe embeddings are stored in a vec0 virtual table
    - Embeddings can be generated from recipe text using the loaded model
    - Embeddings are serialized correctly for sqlite-vec storage
  artifacts:
    - path: alembic/versions/xxx_add_recipe_embeddings.py
      provides: vec0 virtual table for recipe embeddings
      contains: CREATE VIRTUAL TABLE.*recipe_embeddings
    - path: src/eat_it/services/embedding.py
      provides: embedding generation and serialization utilities
      exports: [serialize_f32, generate_embedding, recipe_to_text]
    - path: tests/test_embedding_service.py
      provides: test coverage for embedding service
  key_links:
    - from: src/eat_it/services/embedding.py
      to: app.state.embedding_model
      via: request.app.state.embedding_model
      pattern: SentenceTransformer
---

<objective>
Set up the embedding infrastructure: database table for storing recipe embeddings
and a service module for generating/serializing embeddings.

Purpose: Enable semantic search by providing the storage and generation layer.
Output: Migration for vec0 table, embedding service module with utility functions,
comprehensive test coverage.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-search/03-CONTEXT.md
@.planning/phases/03-semantic-search/03-RESEARCH.md
@src/eat_it/main.py
@src/eat_it/database.py
@src/eat_it/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing tests for embedding service</name>
  <files>tests/test_embedding_service.py</files>
  <behavior>
    - Test serialize_f32: 384 floats produce 1536 bytes (384 * 4)
    - Test serialize_f32: empty list produces empty bytes
    - Test generate_embedding: returns list of 384 floats from mock model
    - Test generate_embedding: raises RuntimeError when model is None
    - Test recipe_to_text: combines title + description + instructions
    - Test recipe_to_text: handles missing description gracefully
  </behavior>
  <action>
    Create test file tests/test_embedding_service.py with failing tests that define
    the expected behavior of the embedding service module.

    Write tests for:

    1. `test_serialize_f32_produces_correct_byte_length`
       - Create list of 384 floats (e.g., [0.1] * 384)
       - Call serialize_f32(vector)
       - Assert result is bytes with length 1536 (384 * 4)

    2. `test_serialize_f32_empty_list`
       - Call serialize_f32([])
       - Assert result is b'' (empty bytes)

    3. `test_generate_embedding_returns_384_dimensions`
       - Create mock SentenceTransformer that returns np.array([0.1] * 384)
       - Call generate_embedding("test text", mock_model)
       - Assert result is list of 384 floats

    4. `test_generate_embedding_raises_on_none_model`
       - Call generate_embedding("test", None)
       - Assert raises RuntimeError with message about model not loaded

    5. `test_recipe_to_text_combines_all_fields`
       - Create mock Recipe with title, description, instructions
       - Call recipe_to_text(recipe)
       - Assert all three fields appear in output, space-separated

    6. `test_recipe_to_text_handles_missing_description`
       - Create mock Recipe with title, instructions, description=None
       - Call recipe_to_text(recipe)
       - Assert only title and instructions in output

    Use unittest.mock for mocking. This test file will FAIL until Task 2 creates
    the implementation module.

    Commit with: test(03-01): add failing tests for embedding service
  </action>
  <verify>
    <automated>pytest tests/test_embedding_service.py -x 2>&1 | grep -E "(FAILED|ERROR|ImportError|ModuleNotFoundError)" && echo "Tests fail as expected (RED)" || echo "Unexpected: tests pass or different error"</automated>
  </verify>
  <done>Test file exists; tests fail because embedding service module does not exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement embedding service to pass tests</name>
  <files>src/eat_it/services/embedding.py</files>
  <behavior>
    - serialize_f32: converts list[float] to bytes using struct.pack
    - generate_embedding: uses model.encode() and converts to list
    - recipe_to_text: joins title, description, instructions with spaces
  </behavior>
  <action>
    Create src/eat_it/services/embedding.py with minimal implementation to pass
    all tests from Task 1.

    The module should provide:

    1. `serialize_f32(vector: list[float]) -> bytes`
       - Import struct at the top
       - Use struct.pack("%sf" % len(vector), *vector)
       - Return the packed bytes
       - Empty list returns b''

    2. `generate_embedding(text: str, model) -> list[float]`
       - Check if model is None, raise RuntimeError("Embedding model not loaded")
       - Call model.encode(text, convert_to_numpy=True)
       - Convert numpy array to list with .tolist()
       - Return the list

    3. `recipe_to_text(recipe) -> str`
       - Start with [recipe.title]
       - If recipe.description exists and is not None, append it
       - Append recipe.instructions
       - Join with " ".join(parts)
       - Return the combined string

    Add proper type hints:
    - from typing import List
    - def serialize_f32(vector: List[float]) -> bytes
    - def generate_embedding(text: str, model) -> List[float]
    - def recipe_to_text(recipe) -> str

    Add docstrings explaining each function's purpose.

    Run tests after implementation to verify GREEN state.

    Commit with: feat(03-01): implement embedding service
  </action>
  <verify>
    <automated>pytest tests/test_embedding_service.py -x -v</automated>
  </verify>
  <done>All embedding service tests pass (GREEN state achieved)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Create vec0 migration with test verification</name>
  <files>alembic/versions/xxx_add_recipe_embeddings.py</files>
  <behavior>
    - Migration upgrade creates vec0 virtual table with FLOAT[384]
    - Migration downgrade drops the table
    - After upgrade, table exists in sqlite_master
  </behavior>
  <action>
    Create an Alembic migration that adds a vec0 virtual table for recipe embeddings.

    First, generate the migration file:
    ```bash
    alembic revision -m "add_recipe_embeddings"
    ```

    Then edit the generated file to include:

    The table structure:
    - Name: recipe_embeddings
    - Uses vec0 virtual table type
    - Single column: embedding FLOAT[384] (all-MiniLM-L6-v2 produces 384 dimensions)
    - rowid will be the recipe_id for JOIN operations

    Migration commands:
    ```python
    def upgrade() -> None:
        op.execute("""
            CREATE VIRTUAL TABLE IF NOT EXISTS recipe_embeddings
            USING vec0(embedding FLOAT[384])
        """)

    def downgrade() -> None:
        op.execute("DROP TABLE IF EXISTS recipe_embeddings")
    ```

    Use raw SQL in the migration since vec0 is a virtual table (not standard SQLAlchemy).
    Follow existing migration patterns from Phase 1.

    Reference RESEARCH.md Pattern 1 for the exact SQL syntax.

    Commit with: feat(03-01): add recipe_embeddings vec0 virtual table
  </action>
  <verify>
    <automated>alembic upgrade head && python -c "from eat_it.database import get_engine; conn = get_engine().raw_connection(); result = conn.execute('SELECT name FROM sqlite_master WHERE type=\"table\" AND name=\"recipe_embeddings\"').fetchone(); print('Table exists:', result is not None)"</automated>
  </verify>
  <done>Migration runs successfully; recipe_embeddings virtual table exists in database</done>
</task>

</tasks>

<verification>
- [ ] Test file created with failing tests for all three functions
- [ ] Tests initially fail (RED state verified)
- [ ] embedding.py module created with three functions
- [ ] All tests pass (GREEN state verified)
- [ ] serialize_f32 produces correct byte length (1536 for 384 floats)
- [ ] Migration file created with vec0 table creation SQL
- [ ] Migration applies successfully (alembic upgrade head)
- [ ] recipe_embeddings virtual table exists in database
- [ ] Functions have proper type hints and docstrings
</verification>

<success_criteria>
- All embedding service tests pass
- Migration runs without errors
- Virtual table queryable via raw SQL
- Embedding service module importable and functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-search/03-01-SUMMARY.md`
</output>
