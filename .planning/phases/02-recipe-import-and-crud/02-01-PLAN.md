---
phase: 02-recipe-import-and-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eat_it/models/recipe.py
  - src/eat_it/schemas/__init__.py
  - src/eat_it/schemas/recipe.py
  - alembic/versions/002_add_recipe_rating_notes.py
autonomous: true
requirements:
  - RECIPE-03
must_haves:
  truths:
    - Recipe model has rating field (nullable int, 1-5)
    - Recipe model has notes field (nullable text)
    - Pydantic schemas exist for RecipeCreate, RecipeUpdate, RecipePublic
    - Database migration applies cleanly
  artifacts:
    - path: src/eat_it/models/recipe.py
      provides: Recipe model with rating/notes
      contains: "rating:"
      contains: "notes:"
    - path: src/eat_it/schemas/recipe.py
      provides: Pydantic request/response schemas
      exports: ["RecipeCreate", "RecipeUpdate", "RecipePublic"]
    - path: alembic/versions/002_add_recipe_rating_notes.py
      provides: Migration for new fields
      contains: "upgrade"
  key_links:
    - from: src/eat_it/schemas/recipe.py
      to: src/eat_it/models/recipe.py
      via: "imports Recipe model"
      pattern: "from eat_it.models"
---

<objective>
Extend Recipe model with rating and notes fields, and create Pydantic schemas
for API request/response validation.

Purpose: Enable users to add private annotations (rating 1-5 stars, freeform
notes) to saved recipes. Establish clean Pydantic schema pattern for CRUD
operations.

Output: Updated Recipe model, new schemas module, database migration.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recipe-import-and-crud/02-CONTEXT.md
@.planning/phases/02-recipe-import-and-crud/02-RESEARCH.md

@src/eat_it/models/recipe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rating and notes fields to Recipe model</name>
  <files>src/eat_it/models/recipe.py</files>
  <action>
Add two new fields to the Recipe model in src/eat_it/models/recipe.py:

1. `rating: Optional[int] = Field(default=None, ge=1, le=5)` - User rating
   1-5 stars, nullable
2. `notes: Optional[str] = None` - Private freeform notes, nullable

Add these fields after the `tags` field, before `metadata_`. Include proper
type hints and Field constraints.

CRITICAL: Do NOT modify the Ingredient or IngredientGroup models - they are
complete as-is from Phase 1.
  </action>
  <verify>
    <automated>python -c "from eat_it.models import Recipe; r = Recipe(title='Test', instructions='Test'); assert hasattr(r, 'rating'); assert hasattr(r, 'notes')" </automated>
    <manual>Verify fields appear in Recipe model with correct types</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Recipe model has rating (int 1-5, nullable) and notes (str, nullable) fields</done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic schemas for Recipe API</name>
  <files>
    src/eat_it/schemas/__init__.py
    src/eat_it/schemas/recipe.py
  </files>
  <action>
Create a new schemas module following the pattern from RESEARCH.md Pattern 1.

1. Create src/eat_it/schemas/__init__.py (empty or with exports)

2. Create src/eat_it/schemas/recipe.py with:

   - RecipeBase: Common fields (title, description, instructions, prep_time,
     cook_time, servings, source_url, image_url, tags)
   - RecipeCreate(RecipeBase): For POST requests - all optional fields truly
     optional, only title and instructions required per CONTEXT.md
   - RecipeUpdate: All fields Optional for partial PATCH updates
   - RecipePublic(RecipeBase): Response model - includes id, rating, notes,
     created_at, updated_at

Key constraints from CONTEXT.md:
- Only title is required (description, instructions, etc. are optional)
- Strict validation - no lenient coercion
- Use Field() for validation constraints (e.g., rating ge=1, le=5)

Do NOT include ingredient fields in schemas yet - ingredients are stored as
plain text per CONTEXT.md "plain text ingredients" decision.
  </action>
  <verify>
    <automated>python -c "from eat_it.schemas.recipe import RecipeCreate, RecipeUpdate, RecipePublic; print('OK") </automated>
    <manual>Verify schemas import correctly and have expected fields</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
RecipeCreate, RecipeUpdate, RecipePublic schemas exist with proper field
definitions and validation
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Alembic migration for new fields</name>
  <files>alembic/versions/002_add_recipe_rating_notes.py</files>
  <action>
Create a new Alembic migration to add rating and notes columns to the recipes
table.

1. Generate migration: `alembic revision -m "add_recipe_rating_notes"`

2. Edit the generated file to add upgrade():
   - Add `rating INTEGER` column (nullable)
   - Add `notes TEXT` column (nullable)

3. Add downgrade():
   - Drop `notes` column
   - Drop `rating` column

4. Test migration works: `alembic upgrade head` (on a test database)

NOTE: Use batch operations for SQLite compatibility if needed:
```python
with op.batch_alter_table('recipes') as batch_op:
    batch_op.add_column(sa.Column('rating', sa.Integer(), nullable=True))
    batch_op.add_column(sa.Column('notes', sa.Text(), nullable=True))
```
  </action>
  <verify>
    <automated>alembic check 2>/dev/null || echo "Migration file created" </automated>
    <manual>Verify migration file exists with upgrade/downgrade</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Migration file exists with upgrade/downgrade for rating and notes columns</done>
</task>

</tasks>

<verification>
After all tasks:
1. Recipe model has rating and notes fields
2. Schemas exist and import without error
3. Migration file exists and is syntactically valid
</verification>

<success_criteria>
- [ ] Recipe model has `rating: Optional[int]` with ge=1, le=5 constraint
- [ ] Recipe model has `notes: Optional[str]`
- [ ] RecipeCreate schema exists with title required, other fields optional
- [ ] RecipeUpdate schema exists with all fields Optional
- [ ] RecipePublic schema exists with id, timestamps, rating, notes
- [ ] Alembic migration file exists for adding columns
</success_criteria>

<output>
After completion, create `.planning/phases/02-recipe-import-and-crud/02-01-SUMMARY.md`
</output>
