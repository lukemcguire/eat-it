---
phase: 05-frontend-and-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/src/App.tsx
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useRecipes.ts
  - frontend/src/index.css
  - frontend/index.html
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - User can run the frontend development server
    - Frontend connects to backend API via fetch
    - TanStack Query provides caching and loading states
    - All recipe API endpoints are accessible via hooks
  artifacts:
    - path: frontend/package.json
      provides: Frontend dependencies
      contains: "@tanstack/react-query"
    - path: frontend/src/lib/api.ts
      provides: API client with base URL
      exports: ["apiClient"]
    - path: frontend/src/hooks/useRecipes.ts
      provides: Recipe API hooks
      exports: ["useRecipes", "useRecipe", "useCreateRecipe"]
    - path: frontend/src/App.tsx
      provides: QueryClient setup
      contains: "QueryClientProvider"
  key_links:
    - from: frontend/src/hooks/useRecipes.ts
      to: frontend/src/lib/api.ts
      via: import apiClient
      pattern: "from.*api"
    - from: frontend/src/App.tsx
      to: frontend/src/hooks/useRecipes.ts
      via: TanStack Query
      pattern: "useRecipes"
---

<objective>
Move frontend from .planning/UI/ to frontend/, add TanStack Query for API
state management, and create typed API hooks for all backend endpoints.

Purpose: Enable production-ready frontend with proper API integration
Output: frontend/ directory with Vite, TanStack Query, and API hooks
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-and-deployment/05-CONTEXT.md
@.planning/phases/05-frontend-and-deployment/05-RESEARCH.md
@.planning/UI/DESIGN.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/eat_it/routers/recipes.py:
```python
# GET /recipes - List recipes with pagination
# Query params: offset (int, ge=0), limit (int, ge=1, le=1000), q (optional search)
# Response: list[RecipePublic]

# GET /recipes/{recipe_id} - Get single recipe
# Response: RecipePublic

# POST /recipes - Create recipe
# Body: RecipeCreate
# Response: RecipePublic (201)

# PATCH /recipes/{recipe_id} - Update recipe
# Body: RecipeUpdate
# Response: RecipePublic

# DELETE /recipes/{recipe_id} - Delete recipe
# Response: 204 No Content

# GET /recipes/parse - Parse recipe URL
# Query params: url (required)
# Response: ParseResponse { success, data, error, duplicate_warning }

# PATCH /recipes/{recipe_id}/rating - Update rating
# Body: RecipeRatingUpdate { rating: int | null }
# Response: RecipePublic

# PATCH /recipes/{recipe_id}/notes - Update notes
# Body: RecipeNotesUpdate { notes: str | null }
# Response: RecipePublic

# GET /recipes/export - Export recipes
# Query params: format (json|csv)
# Response: StreamingResponse (file download)
```

From src/eat_it/schemas/recipe.py:
```python
class RecipePublic:
    id: int
    title: str
    description: str | None
    instructions: str
    prep_time: str | None
    cook_time: str | None
    servings: str | None
    source_url: str | None
    image_url: str | None
    tags: list[str] | None
    rating: int | None
    notes: str | None
    created_at: datetime
    updated_at: datetime
```

From .planning/UI/package.json:
```json
{
  "dependencies": {
    "react": "^18.3.1",
    "@radix-ui/react-*": "various",
    "lucide-react": "^0.460.0",
    "tailwindcss": "^3.4.15"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend directory structure and move prototype files</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/src/</files>
  <action>
    Copy frontend prototype from .planning/UI/ to frontend/:

    1. Create frontend/ directory
    2. Copy these files from .planning/UI/ to frontend/:
       - package.json
       - vite.config.ts
       - tsconfig.json
       - tsconfig.node.json
       - index.html
       - tailwind.config.js
       - postcss.config.js (if exists)
       - components.json (shadcn config)
       - style-guide.json
       - src/ directory (all components, hooks, lib, data)

    3. Update vite.config.ts base path:
       ```typescript
       export default defineConfig({
         plugins: [react()],
         base: '/',  // Root path for static file serving
         server: {
           proxy: {
             '/recipes': 'http://localhost:8000',  // Dev proxy to backend
           }
         }
       });
       ```

    4. Add TanStack Query to package.json dependencies:
       ```json
       "@tanstack/react-query": "^5.60.0"
       ```

    Do NOT modify the existing prototype files in .planning/UI/ - they are reference.
  </action>
  <verify>
    <automated>test -f frontend/package.json && test -f frontend/vite.config.ts && test -d frontend/src/components</automated>
  </verify>
  <done>
    frontend/ directory created with all prototype files copied.
    vite.config.ts has dev proxy to backend.
    TanStack Query added to dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API client with fetch</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
    Create frontend/src/lib/api.ts with typed fetch client:

    ```typescript
    const API_BASE = import.meta.env.VITE_API_BASE || '';

    export class ApiError extends Error {
      constructor(
        public status: number,
        message: string
      ) {
        super(message);
        this.name = 'ApiError';
      }
    }

    export async function apiClient<T>(
      endpoint: string,
      options?: RequestInit
    ): Promise<T> {
      const response = await fetch(`${API_BASE}${endpoint}`, {
        ...options,
        headers: {
          'Content-Type': 'application/json',
          ...options?.headers,
        },
      });

      if (!response.ok) {
        const error = await response.json().catch(() => ({ detail: 'Unknown error' }));
        throw new ApiError(response.status, error.detail || 'Request failed');
      }

      // Handle 204 No Content
      if (response.status === 204) {
        return undefined as T;
      }

      return response.json();
    }
    ```

    Key design decisions:
    - API_BASE empty string means same-origin (production)
    - VITE_API_BASE can be set for development to different backend URL
    - ApiError class preserves HTTP status for error handling
    - 204 No Content handling for DELETE endpoints
  </action>
  <verify>
    <automated>test -f frontend/src/lib/api.ts && grep -q "ApiError" frontend/src/lib/api.ts && grep -q "apiClient" frontend/src/lib/api.ts</automated>
  </verify>
  <done>
    Typed API client with error handling and base URL support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Recipe type definitions</name>
  <files>frontend/src/types/recipe.ts</files>
  <action>
    Create frontend/src/types/recipe.ts matching backend schemas:

    ```typescript
    export interface Recipe {
      id: number;
      title: string;
      description: string | null;
      instructions: string;
      prep_time: string | null;
      cook_time: string | null;
      servings: string | null;
      source_url: string | null;
      image_url: string | null;
      tags: string[] | null;
      rating: number | null;
      notes: string | null;
      created_at: string;
      updated_at: string;
    }

    export interface RecipeCreate {
      title: string;
      description?: string | null;
      instructions: string;
      prep_time?: string | null;
      cook_time?: string | null;
      servings?: string | null;
      source_url?: string | null;
      image_url?: string | null;
      tags?: string[] | null;
    }

    export type RecipeUpdate = Partial<RecipeCreate>;

    export interface RecipeRatingUpdate {
      rating: number | null;
    }

    export interface RecipeNotesUpdate {
      notes: string | null;
    }

    export interface ParseResponse {
      success: boolean;
      data?: Record<string, unknown>;
      error?: {
        code: string;
        message: string;
        suggested_action: string;
      };
      duplicate_warning?: {
        existing_recipe: Recipe;
      };
    }
    ```

    Types match backend Pydantic schemas from src/eat_it/schemas/recipe.py.
  </action>
  <verify>
    <automated>test -f frontend/src/types/recipe.ts && grep -q "export interface Recipe" frontend/src/types/recipe.ts</automated>
  </verify>
  <done>
    TypeScript types matching backend Recipe schemas.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create TanStack Query hooks for Recipe API</name>
  <files>frontend/src/hooks/useRecipes.ts</files>
  <action>
    Create frontend/src/hooks/useRecipes.ts with TanStack Query hooks:

    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
    import { apiClient } from '@/lib/api';
    import type { Recipe, RecipeCreate, RecipeUpdate, RecipeRatingUpdate, RecipeNotesUpdate, ParseResponse } from '@/types/recipe';

    // List recipes with pagination and search
    export function useRecipes(params?: { offset?: number; limit?: number; q?: string }) {
      const searchParams = new URLSearchParams();
      if (params?.offset) searchParams.set('offset', String(params.offset));
      if (params?.limit) searchParams.set('limit', String(params.limit));
      if (params?.q) searchParams.set('q', params.q);

      const query = searchParams.toString();
      return useQuery({
        queryKey: ['recipes', params],
        queryFn: () => apiClient<Recipe[]>(`/recipes${query ? `?${query}` : ''}`),
      });
    }

    // Get single recipe
    export function useRecipe(id: number) {
      return useQuery({
        queryKey: ['recipes', id],
        queryFn: () => apiClient<Recipe>(`/recipes/${id}`),
        enabled: !!id,
      });
    }

    // Create recipe
    export function useCreateRecipe() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: (recipe: RecipeCreate) =>
          apiClient<Recipe>('/recipes/', {
            method: 'POST',
            body: JSON.stringify(recipe),
          }),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['recipes'] });
        },
      });
    }

    // Update recipe
    export function useUpdateRecipe() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ id, recipe }: { id: number; recipe: RecipeUpdate }) =>
          apiClient<Recipe>(`/recipes/${id}`, {
            method: 'PATCH',
            body: JSON.stringify(recipe),
          }),
        onSuccess: (_, { id }) => {
          queryClient.invalidateQueries({ queryKey: ['recipes'] });
          queryClient.invalidateQueries({ queryKey: ['recipes', id] });
        },
      });
    }

    // Delete recipe
    export function useDeleteRecipe() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: (id: number) =>
          apiClient<void>(`/recipes/${id}`, { method: 'DELETE' }),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['recipes'] });
        },
      });
    }

    // Parse recipe URL
    export function useParseRecipe() {
      return useMutation({
        mutationFn: (url: string) =>
          apiClient<ParseResponse>(`/recipes/parse?url=${encodeURIComponent(url)}`),
      });
    }

    // Update rating
    export function useUpdateRating() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ id, rating }: { id: number; rating: RecipeRatingUpdate }) =>
          apiClient<Recipe>(`/recipes/${id}/rating`, {
            method: 'PATCH',
            body: JSON.stringify(rating),
          }),
        onSuccess: (_, { id }) => {
          queryClient.invalidateQueries({ queryKey: ['recipes', id] });
        },
      });
    }

    // Update notes
    export function useUpdateNotes() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ id, notes }: { id: number; notes: RecipeNotesUpdate }) =>
          apiClient<Recipe>(`/recipes/${id}/notes`, {
            method: 'PATCH',
            body: JSON.stringify(notes),
          }),
        onSuccess: (_, { id }) => {
          queryClient.invalidateQueries({ queryKey: ['recipes', id] });
        },
      });
    }
    ```

    All hooks follow TanStack Query v5 patterns with proper cache invalidation.
  </action>
  <verify>
    <automated>test -f frontend/src/hooks/useRecipes.ts && grep -q "useQuery" frontend/src/hooks/useRecipes.ts && grep -q "useMutation" frontend/src/hooks/useRecipes.ts</automated>
  </verify>
  <done>
    TanStack Query hooks for all recipe CRUD operations with cache invalidation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Set up QueryClient in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Update frontend/src/App.tsx to wrap app with QueryClientProvider:

    ```typescript
    import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

    const queryClient = new QueryClient({
      defaultOptions: {
        queries: {
          staleTime: 5 * 60 * 1000, // 5 minutes
          retry: 1,
        },
      },
    });

    export function App() {
      return (
        <QueryClientProvider client={queryClient}>
          {/* Existing app content */}
        </QueryClientProvider>
      );
    }
    ```

    Preserve any existing app structure. Add QueryClientProvider as outer wrapper.
  </action>
  <verify>
    <automated>test -f frontend/src/App.tsx && grep -q "QueryClientProvider" frontend/src/App.tsx</automated>
  </verify>
  <done>
    App wrapped with QueryClientProvider for TanStack Query.
  </done>
</task>

</tasks>

<verification>
1. Frontend builds successfully: `cd frontend && npm run build`
2. API client exists with typed fetch wrapper
3. Recipe types match backend schemas
4. TanStack Query hooks for all CRUD operations
5. QueryClientProvider wraps app
</verification>

<success_criteria>
- frontend/ directory contains production-ready React app
- TanStack Query manages all API state with caching
- Type-safe API client with error handling
- All recipe endpoints accessible via hooks
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-and-deployment/05-02-SUMMARY.md`
</output>
