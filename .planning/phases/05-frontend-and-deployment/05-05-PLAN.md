---
phase: 05-frontend-and-deployment
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/index.ts
  - frontend/src/vite-env.d.ts
  - frontend/src/components/recipe-import/RecipeImportScreen.tsx
  - .planning/phases/05-frontend-and-deployment/05-UAT.md
autonomous: true
requirements: [DATA-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Docker build completes without TypeScript compilation errors"
    - "Frontend TypeScript recognizes import.meta.env type declarations"
    - "No unused import warnings cause build failures"
  artifacts:
    - path: "frontend/src/components/ui/index.ts"
      provides: "Correct export of Icon component"
      exports: ["Icon"]
    - path: "frontend/src/vite-env.d.ts"
      provides: "Vite type declarations for import.meta.env"
      contains: "reference types=\"vite/client\""
    - path: "frontend/src/components/recipe-import/RecipeImportScreen.tsx"
      provides: "Clean imports without unused variables"
  key_links:
    - from: "frontend/src/components/ui/index.ts"
      to: "frontend/src/components/ui/IconButton.tsx"
      via: "export statement"
      pattern: "export.*Icon.*from.*IconButton"
---

<objective>
Fix TypeScript compilation errors blocking Docker build.

Purpose: Enable successful Docker image builds for DATA-02 self-hosting
requirement.
Output: Clean TypeScript build with no compilation errors.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-and-deployment/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Icon export in index.ts</name>
  <files>frontend/src/components/ui/index.ts</files>
  <action>
    The file `frontend/src/components/ui/index.ts` exports `IconButton` but
    `IconButton.tsx` only exports `Icon`. Fix the export statement:

    Change line 3 from:
    ```
    export { IconButton } from './IconButton';
    ```

    To:
    ```
    export { Icon } from './IconButton';
    ```

    The file `IconButton.tsx` exports `Icon` component (not `IconButton`), so
    the re-export must match.
  </action>
  <verify>
    <automated>grep -q "export { Icon } from './IconButton'" frontend/src/components/ui/index.ts</automated>
  </verify>
  <done>index.ts exports Icon correctly matching IconButton.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Create vite-env.d.ts for TypeScript type declarations</name>
  <files>frontend/src/vite-env.d.ts</files>
  <action>
    Create `frontend/src/vite-env.d.ts` with Vite type declarations. This file
    enables TypeScript to recognize `import.meta.env` and other Vite-specific
    types.

    File content:
    ```typescript
    /// <reference types="vite/client" />
    ```
  </action>
  <verify>
    <automated>test -f frontend/src/vite-env.d.ts && grep -q 'reference types="vite/client"' frontend/src/vite-env.d.ts</automated>
  </verify>
  <done>vite-env.d.ts exists with Vite client type reference</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused StepNumber import from RecipeImportScreen</name>
  <files>frontend/src/components/recipe-import/RecipeImportScreen.tsx</files>
  <action>
    Remove unused `StepNumber` import from line 3.

    Change from:
    ```typescript
    import { Icon, Button, Input, Tag, StepNumber, Card } from '../ui';
    ```

    To:
    ```typescript
    import { Icon, Button, Input, Tag, Card } from '../ui';
    ```

    The component uses an inline `ImportStep` component with its own step number
    styling, not the `StepNumber` component from ui/.
  </action>
  <verify>
    <automated>! grep -q "StepNumber" frontend/src/components/recipe-import/RecipeImportScreen.tsx</automated>
  </verify>
  <done>StepNumber import removed from RecipeImportScreen.tsx</done>
</task>

<task type="auto">
  <name>Task 4: Update UAT.md to document design choice for Gap 2</name>
  <files>.planning/phases/05-frontend-and-deployment/05-UAT.md</files>
  <action>
    Update the "Frontend App Loads" test (Test 4) to document that the SPA
    fallback behavior is by design, not a bug.

    For the second gap in the Gaps section, update the status from "failed" to
    "by_design" and add a design_note field explaining:

    - The SPA fallback in main.py only serves the frontend when `static/`
      directory exists (production mode)
    - In development, the intended workflow is to use Vite's dev server
      (port 5173) with API proxy to FastAPI backend
    - This is working as designed - not a bug
    - For dev workflow: Run `cd frontend && pnpm dev` separately from FastAPI
    - For production: Build frontend first, then Docker will serve it

    Update the gap's status to "by_design" and add the design_note field with
    the explanation above.
  </action>
  <verify>
    <automated>grep -q "by_design" .planning/phases/05-frontend-and-deployment/05-UAT.md</automated>
  </verify>
  <done>UAT.md documents that SPA fallback is production-only by design</done>
</task>

</tasks>

<verification>
After all tasks complete, verify Docker build succeeds:

```bash
cd /home/luke/workspace/github.com/lukemcguire/eat-it
docker build -f docker/Dockerfile -t eat-it-gap-test . 2>&1 | tail -20
```

Expected: Build completes without TypeScript compilation errors.
</verification>

<success_criteria>
- Icon export matches IconButton.tsx actual export
- vite-env.d.ts exists with Vite client type reference
- No unused imports in RecipeImportScreen.tsx
- UAT.md documents SPA fallback as production-only design choice
- Docker build completes without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-and-deployment/05-05-SUMMARY.md`
</output>
