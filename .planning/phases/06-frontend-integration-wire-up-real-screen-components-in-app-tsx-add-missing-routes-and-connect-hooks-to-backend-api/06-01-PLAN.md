---
phase: 06-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/RecipeDetailModal.tsx
  - frontend/package.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User sees toast notification when API errors occur"
    - "Home route (/) redirects to recipe binder (/recipes)"
    - "All four main routes are accessible (recipes, shopping, search, import)"
    - "Recipe detail modal opens with ?recipe={id} query parameter"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Route definitions and toast provider"
      contains: "Route path="
    - path: "frontend/src/components/RecipeDetailModal.tsx"
      provides: "Modal overlay for recipe detail"
      min_lines: 20
    - path: "frontend/package.json"
      provides: "Sonner dependency"
      contains: "sonner"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "sonner Toaster"
      via: "import and render"
      pattern: "Toaster"
    - from: "App routes"
      to: "screen components"
      via: "Route element"
      pattern: "element="
---

<objective>
Install toast notification infrastructure and set up complete route structure with
modal-based recipe detail pattern.

Purpose: Establish the navigation foundation and error feedback mechanism that all
other features depend on.
Output: Routes configured, toast notifications working, recipe detail modal
component.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-CONTEXT.md
@.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-RESEARCH.md

<interfaces>
<!-- Existing types and patterns from codebase -->

From frontend/src/types/recipe.ts:
```typescript
export interface Recipe {
  id: number;
  title: string;
  description: string | null;
  instructions: string;
  prep_time: string | null;
  cook_time: string | null;
  servings: string | null;
  source_url: string | null;
  image_url: string | null;
  tags: string[] | null;
  rating: number | null;
  notes: string | null;
  created_at: string;
  updated_at: string;
}
```

From frontend/src/hooks/useRecipes.ts:
```typescript
export function useRecipe(id: number);
export function useRecipes(params?: { offset?: number; limit?: number; q?: string; });
```

From frontend/src/lib/api.ts:
```typescript
export class ApiError extends Error {
  constructor(public status: number, message: string);
}
export async function apiClient<T>(endpoint: string, options?: RequestInit): Promise<T>;
```

From frontend/src/components/layout/AppLayout.tsx:
```typescript
export function AppLayout({ title?: string; actions?: React.ReactNode });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install sonner and add Toaster to App</name>
  <files>frontend/package.json, frontend/src/App.tsx</files>
  <behavior>
    - Test 1: Toaster component renders in DOM
    - Test 2: toast.error() displays notification
  </behavior>
  <action>
    1. Install sonner: `cd frontend && npm install sonner`
    2. In App.tsx, import { Toaster } from 'sonner'
    3. Add `<Toaster position="bottom-center" richColors />` inside
       QueryClientProvider, before BrowserRouter
    4. Export toast function for use in error handling

    DO NOT add toast notifications to API calls yet - that comes in later plans.
    Just set up the infrastructure.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run App.test.tsx</automated>
  </verify>
  <done>Sonner installed, Toaster renders in App, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update route structure per CONTEXT.md</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/BottomNav.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <action>
    Update routes in App.tsx to match CONTEXT.md locked decisions:

    1. Change root path from `/` to `/recipes` for RecipeBinder
    2. Add redirect: `path="/"` element={`<Navigate to="/recipes" replace />`}
    3. Change `/shopping` to `/shopping/:id?` (optional id param)
    4. Keep `/search` and `/import` as-is

    Update navigation components to match:
    - BottomNav.tsx: Update paths (recipe binder at `/recipes`, not `/`)
    - Sidebar.tsx: Update paths to match

    Import Navigate from react-router-dom for the redirect.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run App.test.tsx</automated>
  </verify>
  <done>Route structure matches CONTEXT.md, navigation items link correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create RecipeDetailModal component</name>
  <files>frontend/src/components/RecipeDetailModal.tsx</files>
  <action>
    Create a modal overlay component for recipe detail that:

    1. Uses useSearchParams from react-router-dom to read ?recipe={id}
    2. Uses useRecipe hook (already exists) to fetch recipe data
    3. Renders as a full-screen overlay with semi-transparent backdrop
    4. Shows loading state while fetching
    5. Closes when backdrop is clicked (clears search params)
    6. Accepts onClose callback for parent to handle close

    Props interface:
    ```typescript
    interface RecipeDetailModalProps {
      onClose: () => void;
    }
    ```

    The component reads recipe ID from URL params internally, not via props.
    This follows the CONTEXT.md pattern of URL-driven modal state.

    Style: Full-screen overlay with centered content card, dark backdrop with
    opacity, close button in top-right corner. Reuse existing UI components
    where possible (Card, Button from ui/).

    DO NOT integrate with RecipeBinderScreen yet - that comes in plan 03.
    Just create the component.
  </action>
  <verify>
    <automated>MISSING - Wave 0 must create RecipeDetailModal.test.tsx first</automated>
  </verify>
  <done>RecipeDetailModal component created with URL param reading and close
  behavior</done>
</task>

</tasks>

<verification>
- All tests pass: `cd frontend && npm test -- --run`
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- Routes accessible: Manual check / redirects to /recipes
</verification>

<success_criteria>
- Sonner installed and Toaster renders
- Routes match CONTEXT.md structure (/recipes, /shopping/:id, /search, /import)
- RecipeDetailModal component exists with URL param reading
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-01-SUMMARY.md`
</output>
