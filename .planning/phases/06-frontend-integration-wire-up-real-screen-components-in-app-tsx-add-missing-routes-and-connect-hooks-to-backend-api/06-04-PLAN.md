---
phase: 06-frontend-integration
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - frontend/src/components/screens/SearchScreen.tsx
  - frontend/src/App.tsx
  - frontend/src/hooks/index.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Search page accepts queries and shows semantic search results"
    - "All navigation flows work correctly"
    - "Hardware back button closes modal on mobile"
    - "Full application is usable end-to-end"
  artifacts:
    - path: "frontend/src/components/screens/SearchScreen.tsx"
      provides: "Dedicated search page with results"
      min_lines: 40
    - path: "frontend/src/App.tsx"
      provides: "Complete routing with all screens"
      contains: "SearchScreen"
  key_links:
    - from: "SearchScreen"
      to: "useSearch hook"
      via: "import and use"
      pattern: "useSearch"
    - from: "SearchScreen"
      to: "RecipeBinderScreen pattern"
      via: "reuse recipe card grid"
      pattern: "RecipeCard"
---

<objective>
Create dedicated search screen and perform final integration verification with
human testing.

Purpose: Complete the navigation flow with search functionality and verify the
entire application works end-to-end.
Output: Search screen, complete routing, human-verified functionality.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-CONTEXT.md
@.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-RESEARCH.md

<interfaces>
<!-- Hooks and components available -->

From frontend/src/hooks/useSearch.ts (plan 06-02):
```typescript
export function useSearch(query: string, enabled?: boolean);
// Returns { data: Recipe[], isLoading, error }
```

From frontend/src/types/recipe.ts:
```typescript
export interface Recipe {
  id: number;
  title: string;
  description: string | null;
  image_url: string | null;
  // ... other fields
}
```

From CONTEXT.md search implementation:
- Manual trigger (Enter key or button click)
- Full page with search bar at top
- Results as recipe cards grid
- Empty state (before search): Show recent recipes or prompt
- No results state: Friendly message
- Reuse RecipeBinderScreen card layout pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchScreen component</name>
  <files>frontend/src/components/screens/SearchScreen.tsx</files>
  <action>
    Create frontend/src/components/screens/SearchScreen.tsx:

    ```typescript
    import React, { useState, useCallback } from 'react';
    import { useNavigate, useSearchParams } from 'react-router-dom';
    import { useSearch } from '@/hooks/useSearch';
    import { useRecipes } from '@/hooks/useRecipes';
    import { toast } from 'sonner';
    import { Icon, Button, Input } from '@/components/ui';

    export const SearchScreen: React.FC = () => {
      const [query, setQuery] = useState('');
      const [searchTriggered, setSearchTriggered] = useState(false);
      const navigate = useNavigate();

      // Only search when triggered (manual, not live)
      const { data: results, isLoading, error } = useSearch(query, searchTriggered);

      // Recent recipes for empty state
      const { data: recentRecipes } = useRecipes({ limit: 6 });

      // Handle errors
      React.useEffect(() => {
        if (error) {
          toast.error('Search failed');
        }
      }, [error]);

      const handleSearch = useCallback(() => {
        if (query.trim()) {
          setSearchTriggered(true);
        }
      }, [query]);

      const handleKeyDown = useCallback((e: React.KeyboardEvent) => {
        if (e.key === 'Enter') {
          handleSearch();
        }
      }, [handleSearch]);

      const handleRecipeClick = useCallback((id: number) => {
        navigate(`/recipes?recipe=${id}`);
      }, [navigate]);

      const recipesToShow = searchTriggered ? results : recentRecipes?.data;

      return (
        <div className="min-h-screen bg-[#0f1923] p-4 lg:p-6">
          {/* Search Bar */}
          <div className="max-w-3xl mx-auto mb-8">
            <div className="flex gap-2">
              <Input
                value={query}
                onChange={setQuery}
                onKeyDown={handleKeyDown}
                placeholder="Search recipes (e.g., 'quick chicken dinner')"
                icon="search"
                className="flex-1"
              />
              <Button icon="search" onClick={handleSearch} disabled={!query.trim()}>
                Search
              </Button>
            </div>
          </div>

          {/* Results */}
          <div className="max-w-7xl mx-auto">
            {isLoading ? (
              <div className="text-center text-slate-400 py-12">
                Searching...
              </div>
            ) : (
              <>
                {/* Header */}
                {searchTriggered && (
                  <h2 className="text-xl font-bold text-white mb-4">
                    {results?.length ?? 0} result{(results?.length ?? 0) !== 1 ? 's' : ''}
                    for &quot;{query}&quot;
                  </h2>
                )}

                {!searchTriggered && (
                  <h2 className="text-xl font-bold text-white mb-4">
                    Recent Recipes
                  </h2>
                )}

                {/* Recipe Grid - reuse pattern from RecipeBinderScreen */}
                {recipesToShow && recipesToShow.length > 0 ? (
                  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    {recipesToShow.map((recipe) => (
                      <div
                        key={recipe.id}
                        onClick={() => handleRecipeClick(recipe.id)}
                        className="cursor-pointer group"
                      >
                        <div className="bg-slate-900 rounded-xl overflow-hidden border border-slate-800 hover:shadow-xl transition-all">
                          <div className="aspect-[4/3] bg-slate-800">
                            {recipe.image_url ? (
                              <img
                                src={recipe.image_url}
                                alt={recipe.title}
                                className="w-full h-full object-cover group-hover:scale-105 transition-transform"
                              />
                            ) : (
                              <div className="w-full h-full flex items-center justify-center">
                                <Icon name="restaurant" className="text-slate-600 text-4xl" />
                              </div>
                            )}
                          </div>
                          <div className="p-4">
                            <h3 className="font-bold text-white group-hover:text-[#207fdf] transition-colors">
                              {recipe.title}
                            </h3>
                            {recipe.description && (
                              <p className="text-slate-400 text-sm mt-1 line-clamp-2">
                                {recipe.description}
                              </p>
                            )}
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                ) : searchTriggered ? (
                  <div className="text-center py-12">
                    <Icon name="search_off" className="text-slate-600 text-5xl mb-4" />
                    <p className="text-slate-400 text-lg">
                      No recipes found for &quot;{query}&quot;
                    </p>
                    <p className="text-slate-500 text-sm mt-2">
                      Try a different search term or browse your recipe binder
                    </p>
                  </div>
                ) : null}
              </>
            )}
          </div>
        </div>
      );
    };

    export default SearchScreen;
    ```

    Per CONTEXT.md:
    - Manual trigger (Enter or button), not live debounce
    - Shows recent recipes before search
    - Friendly no-results state
    - Clicking recipe navigates to /recipes?recipe={id}
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>SearchScreen created with manual trigger and result grid</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx with SearchScreen and final routes</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Update App.tsx to use SearchScreen instead of placeholder:

    1. Import SearchScreen from '@/components/screens/SearchScreen'
    2. Replace Search placeholder with SearchScreen component
    3. Ensure all routes use real screen components:
       - `/recipes` -> RecipeBinderScreen
       - `/shopping/:id?` -> ShoppingListScreen
       - `/search` -> SearchScreen
       - `/import` -> RecipeImportScreen

    The route structure should be complete with no placeholder components.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <done>All routes use real screen components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete frontend integration with all screens connected to backend
  API, toast notifications for errors, modal-based recipe detail with URL state,
  and semantic search functionality.</what-built>
  <how-to-verify>
    1. Start backend: `cd /home/luke/workspace/github.com/lukemcguire/eat-it && uv run fastapi dev src/eat_it/main.py`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open browser to http://localhost:5173

    Verify each flow:

    **Recipe Binder:**
    - [ ] Home (/) redirects to /recipes
    - [ ] Recipe cards load from API (not mock data)
    - [ ] Clicking a recipe opens modal overlay
    - [ ] URL changes to /recipes?recipe={id}
    - [ ] Modal shows recipe details from API
    - [ ] Clicking backdrop or close closes modal
    - [ ] Hardware back button (browser back) closes modal

    **Recipe Import:**
    - [ ] Navigate to /import
    - [ ] Enter a recipe URL and click Fetch
    - [ ] Preview shows parsed recipe data
    - [ ] Save creates recipe and shows success toast
    - [ ] Navigates back to recipe binder

    **Shopping List:**
    - [ ] Navigate to /shopping (or /shopping/1)
    - [ ] List items load from API
    - [ ] Checking/unchecking item persists to backend
    - [ ] Error shows toast notification

    **Search:**
    - [ ] Navigate to /search
    - [ ] Shows recent recipes before searching
    - [ ] Type query and press Enter or click Search
    - [ ] Results show matching recipes
    - [ ] No results shows friendly message
    - [ ] Clicking result opens recipe modal

    **Error Handling:**
    - [ ] Stop backend and try to load recipes
    - [ ] Toast notification shows error message
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `cd frontend && npm test -- --run`
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- Human verification of all user flows
</verification>

<success_criteria>
- SearchScreen created and integrated
- All four main routes use real components
- All API integrations working
- Error notifications display correctly
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-integration-wire-up-real-screen-components-in-app-tsx-add-missing-routes-and-connect-hooks-to-backend-api/06-04-SUMMARY.md`
</output>
