---
phase: 01-foundation-and-data-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/eat_it/services/__init__.py
  - src/eat_it/services/importer_registry.py
  - src/eat_it/routers/__init__.py
  - src/eat_it/routers/health.py
  - src/eat_it/main.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
user_setup: []

must_haves:
  truths:
    - FastAPI application starts with lifespan context
    - Embedding model loads at startup (eager load)
    - Importer registry discovers plugins via entry points
    - Health endpoint returns 200 OK
    - Application can be run with uvicorn
  artifacts:
    - path: src/eat_it/main.py
      provides: FastAPI app with lifespan
      exports: ["app"]
      contains: "lifespan"
    - path: src/eat_it/services/importer_registry.py
      provides: Plugin discovery via entry points
      exports: ["ImporterRegistry"]
      contains: "entry_points"
    - path: src/eat_it/routers/health.py
      provides: Health check endpoint
      exports: ["router"]
  key_links:
    - from: src/eat_it/main.py
      to: src/eat_it/services/importer_registry.py
      via: import
      pattern: "ImporterRegistry"
    - from: src/eat_it/main.py
      to: sentence_transformers
      via: import
      pattern: "SentenceTransformer"
    - from: src/eat_it/services/importer_registry.py
      to: importlib.metadata
      via: entry_points
      pattern: "eat_it.importers"
---

<objective>
Create the FastAPI application with lifespan context that initializes the
database, loads the embedding model, and sets up the importer registry for
plugin discovery via entry points.

Purpose: Establish the application skeleton that handles startup/shutdown
lifecycle and provides the plugin architecture foundation.
Output: Runnable FastAPI app with health endpoint and plugin system.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@//home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImporterRegistry with entry point discovery</name>
  <files>
    src/eat_it/services/__init__.py
    src/eat_it/services/importer_registry.py
  </files>
  <action>
    Create src/eat_it/services/importer_registry.py:

    **ImporterPlugin Protocol:**
    - name: str
    - can_parse(url: str) -> bool
    - async parse(url: str) -> Recipe (forward reference)

    **ImporterRegistry class:**
    - _importers: Dict[str, ImporterPlugin] private storage
    - discover_plugins() method:
      - Use importlib.metadata.entry_points(group="eat_it.importers")
      - Load each entry point, instantiate class
      - Register by name in _importers dict
      - Catch and log errors (don't crash on plugin load failure)
    - can_parse(url: str) -> bool: Check if any importer handles URL
    - async parse(url: str) -> Optional[Recipe]: Find matching importer
      and call parse()
    - get_importers() -> List[str]: Return registered importer names

    Create src/eat_it/services/__init__.py exporting ImporterRegistry.

    Reference RESEARCH.md Pattern 4 for entry_points usage.
  </action>
  <verify>
    <automated>python -c "from eat_it.services.importer_registry import ImporterRegistry; r = ImporterRegistry(); print(type(r._importers))" | grep "dict"</automated>
  </verify>
  <done>
    ImporterRegistry class exists with all methods.
    discover_plugins() uses entry_points correctly.
    Can be imported from eat_it.services.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create health router</name>
  <files>
    src/eat_it/routers/__init__.py
    src/eat_it/routers/health.py
  </files>
  <action>
    Create src/eat_it/routers/health.py:

    - Create APIRouter with prefix="/health" and tags=["health"]
    - GET "/" endpoint returning:
      ```python
      {
          "status": "healthy",
          "database": "connected",  # Check DB connectivity
          "embedding_model": "loaded"  # Check model is loaded
      }
      ```
    - Endpoint should verify database is accessible and embedding model
      exists in app.state

    Create src/eat_it/routers/__init__.py exporting health router.
  </action>
  <verify>
    <automated>python -c "from eat_it.routers.health import router; print(router.prefix)" | grep "/health"</automated>
  </verify>
  <done>
    Health router exists with GET / endpoint.
    Router prefix is /health.
    Returns structured health status.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create FastAPI app with lifespan context</name>
  <files>
    src/eat_it/main.py
  </files>
  <action>
    Create src/eat_it/main.py:

    **lifespan async context manager:**
    - Startup:
      1. Initialize database (call init_db from database.py)
      2. Load embedding model from sentence_transformers:
         - Store in app.state.embedding_model
         - Use model name from settings.embedding_model
      3. Create ImporterRegistry and discover plugins:
         - Store in app.state.importer_registry
    - Yield (app runs here)
    - Shutdown: (cleanup if needed, SQLite handles itself)

    **FastAPI app:**
    - Create with title="Eat It", version="0.1.0"
    - Pass lifespan=lifespan
    - Include health router
    - Add get_session dependency injection for routes

    Reference RESEARCH.md Pattern 1 for lifespan structure.
    Do NOT use deprecated @app.on_event("startup").
  </action>
  <verify>
    <automated>python -c "from eat_it.main import app; print(app.title)" | grep "Eat It"</automated>
  </verify>
  <done>
    FastAPI app exists with lifespan context.
    App imports and creates successfully.
    Health router is included.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify application starts end-to-end</name>
  <files>
    src/eat_it/main.py
  </files>
  <action>
    Test the complete startup sequence:

    1. Start app with uvicorn (in background or via TestClient)
    2. Verify health endpoint returns 200 with all checks passing
    3. Verify app.state has embedding_model and importer_registry

    If TestClient is used:
    ```python
    from fastapi.testclient import TestClient
    from eat_it.main import app

    client = TestClient(app)
    response = client.get("/health/")
    assert response.status_code == 200
    assert response.json()["status"] == "healthy"
    ```

    Also verify uvicorn can start the app:
    uvicorn eat_it.main:app --host 0.0.0.0 --port 8000 &
    curl http://localhost:8000/health/
    kill the process
  </action>
  <verify>
    <automated>python -c "from fastapi.testclient import TestClient; from eat_it.main import app; client = TestClient(app); r = client.get('/health/'); assert r.status_code == 200; print(r.json())" | grep "healthy"</automated>
  </verify>
  <done>
    Application starts without errors.
    Health endpoint returns 200 with status=healthy.
    All lifespan initialization completes successfully.
  </done>
</task>

</tasks>

<verification>
Start the application with uvicorn and curl the health endpoint.
Verify all lifespan components initialize correctly.
</verification>

<success_criteria>
- FastAPI app starts with lifespan context
- Database initializes with WAL mode
- Embedding model loads at startup (app.state.embedding_model exists)
- Importer registry initializes (app.state.importer_registry exists)
- GET /health/ returns 200 with {"status": "healthy", ...}
- uvicorn eat_it.main:app runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-03-SUMMARY.md`
</output>
