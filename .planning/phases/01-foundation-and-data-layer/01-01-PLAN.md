---
phase: 01-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/eat_it/__init__.py
  - src/eat_it/config.py
autonomous: true
requirements:
  - ARCH-02
user_setup: []

must_haves:
  truths:
    - Developer can install project dependencies with uv
    - Application reads configuration from environment variables
    - Entry points structure exists for plugin discovery
  artifacts:
    - path: pyproject.toml
      provides: Package configuration and dependencies
      contains: "sqlmodel"
      contains: "project.entry-points"
    - path: src/eat_it/config.py
      provides: Pydantic settings from environment
      exports: ["Settings", "settings"]
  key_links:
    - from: src/eat_it/config.py
      to: environment
      via: pydantic-settings
      pattern: "BaseSettings"
---

<objective>
Set up Python package configuration, dependencies, and environment-based
configuration using pydantic-settings.

Purpose: Establish the project foundation with proper dependency management
and 12-factor app configuration pattern.
Output: Installable package with all dependencies and typed configuration.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package structure and pyproject.toml</name>
  <files>
    pyproject.toml
    src/eat_it/__init__.py
  </files>
  <action>
    Create pyproject.toml with:

    - Project metadata (name: eat-it, version: 0.1.0, python >=3.11)
    - Core dependencies:
      - sqlmodel>=0.0.16
      - alembic>=1.13
      - fastapi>=0.115
      - uvicorn>=0.30
      - aiosqlite>=0.20
      - sqlite-vec>=0.1.0
      - sentence-transformers>=3.0
      - pydantic-settings>=2.0
    - Dev dependencies (in [project.optional-dependencies] dev):
      - pytest>=7.0
      - pytest-asyncio>=0.23
      - httpx>=0.27
    - Entry points section for plugins (empty for now):
      ```toml
      [project.entry-points."eat_it.importers"]
      # Built-in importers will be added in Phase 2
      ```
    - Build system with hatchling

    Create src/eat_it/__init__.py with version constant.

    Reference RESEARCH.md for exact versions and patterns.
  </action>
  <verify>
    <automated>uv pip install -e . && uv pip list | grep -E "sqlmodel|fastapi|alembic"</automated>
  </verify>
  <done>
    Package installs successfully with all core dependencies listed.
    Entry points section exists in pyproject.toml.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configuration module</name>
  <files>
    src/eat_it/config.py
  </files>
  <action>
    Create src/eat_it/config.py with Pydantic Settings class:

    - Settings class inheriting from BaseSettings
    - Environment variables (all with EAT_IT_ prefix):
      - EAT_IT_DATABASE_URL: str = "sqlite:///./data/eat-it.db"
      - EAT_IT_EMBEDDING_MODEL: str = "all-MiniLM-L6-v2"
    - model_config with env_file = ".env" and env_prefix = "EAT_IT_"
    - Global settings instance: `settings = Settings()`

    Use pydantic-settings BaseSettings pattern from RESEARCH.md.
  </action>
  <verify>
    <automated>python -c "from eat_it.config import settings; print(settings.database_url)" | grep "sqlite"</automated>
  </verify>
  <done>
    Settings class loads defaults correctly.
    Environment variables override defaults when set.
  </done>
</task>

</tasks>

<verification>
Run installation and import tests to verify package is correctly configured.
</verification>

<success_criteria>
- uv pip install -e . completes without errors
- All core dependencies are installed
- from eat_it.config import settings works
- settings.database_url returns default value
- pyproject.toml contains [project.entry-points."eat_it.importers"]
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-01-SUMMARY.md`
</output>
